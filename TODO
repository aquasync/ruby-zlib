= TODO

- fix incremental deflation.
- fix the simple speed problems.
- package works now, but check that gem unpack works also.
- merge the huffman implementation used for inflate with
  the deflate one.
- merge other static data like codelen tables etc.
- migrate the bit reading.

